# CaP-X × UniVTAC 触觉插入实验汇报

## 一、背景与目标

- **CaP-X**:基于代码生成的机器人控制框架 —— LLM 生成 Python 策略代码,通过高层 API 闭环执行
- **UniVTAC**:原生触觉仿真环境(gsmini 传感器),提供 slip/压力象限/形变等触觉信号
- **目标**:在官方 `insert_hole` 协议下,仅靠原生触觉反馈完成试管插入(600 动作预算;成功 = 相对目标姿态插深 ≥4cm 且轴向对齐)

## 二、实验内容

LLM 每步读取触觉摘要(slip_score / centroid / quadrant_pressure / pitch_couple),在 guarded 微步移动(触觉保护)下决策:下插、方向校正、撤回。共 6 轮迭代实验。

## 三、实验结果

| 轮次 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 74 动作后中断,插深 34.8mm | slip 硬阈值 0.60 过紧 | 阈值分级 0.45/0.68/0.72 |
| 2 | 5 动作后 no_progress 停止 | prompt 标定数值 + 候选数上限,限制 LLM 泛化 | 重写为无数值、无上限的持续探索策略 |
| 3 | 首迭代即退出(budget_exhausted) | LLM 从触觉摘要读预算字段(不存在→0) | prompt 增加字段来源规则 |
| 4 | 代码崩溃(TypeError) | quadrant_pressure 实为 dict 而非数组 | prompt 增加字段类型契约 |
| 5 | 墙钟超时,79 动作仅 30.6mm | 每调用 ~7.6s(物理仿真慢),600s 只够 ~79 次调用 | 微步 0.001→0.003,超时 600→900s |
| 6 | 滑动早停,插深 57mm(世界系) | 方向从未校正 + 持续硬压 | 已定位方案,待验证 |

## 四、关键发现

1. **guard 微步压缩机制**:接触中 risk 恒为 medium,每次调用仅执行 1 个微步(下插 1mm / 旋转 0.003rad);转满 30° 需 ~175 次调用,动作预算内不可能 —— 这是此前所有轮次失败的根因
2. **官方成功姿态是 ±30° 倾斜插入**(`target_pose = hole_pose.add_rotation([0, ∓π/6, 0])`),纯竖直插入永远无法满足轴对齐判据;本实验 seed 下正确方向为 pitch -30°
3. **方向从未被执行**:144 次调用累计净 pitch = 0°(正负微步互相抵消),最终姿态相对目标残余 26.7°
4. **滑动机理**:方向错位状态下持续下压 → 试管斜顶孔壁 → 轴向反力使试管在手指间滑移(inhand_bias 越过官方 0.04 阈值)→ 判定失败终止
5. **突破方案已定位**:`goto_pose` 绝对位姿 API 不受 guard 压缩,30° 旋转 3 步即可一次到位,随后 guarded 下插 —— 绕开方向搜索的预算死结

## 五、辅助产出

- 运行日志摘要工具 `univtac_run_summary.py`(动作/角度/守护阶段一键可视化)
- 触觉语言模型调研:Octopi / UniTouch(材质分类,对口 grasp_classify 任务)、VTLA / OmniVTA(插入与接触预测,备选)

## 六、下一步

1. 验证 goto_pose 闭环:初始姿态设为 -30° → guarded 下插,确认"方向正确时系统可完成插入"
2. 机制打通后,回归方向搜索策略(微步已放大 3 倍,角度可达性大幅提升)
3. classify 任务引入触觉语言模型做零样本材质分类对比
